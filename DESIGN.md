```markdown
# 视觉设计系统指南：极致简约与原生生产力

作为设计师，我们的目标不是在屏幕上堆砌组件，而是为用户构建一个“透明”的生产力环境。本设计系统深度融合了 Windows 11 的现代美学与极致的工具实用主义。我们抛弃了一切社交冗余，专注于**沉浸、无干扰、原生感**。

这份文档将指导你如何利用“影调层次”而非“线条构造”来构建一个高端、专业的本地化工具体验。

---

## 1. 核心设计北极星：数字策展人 (The Digital Curator)

本系统的核心理念是**“数字策展人”**。这意味着界面应当像一座极简主义的现代美术馆：墙板（背景）是安静的，光影（材质）是流动的，所有的注意力都应当被引导至内容本身。

我们通过**有意的不对称布局**和**大面积的留白**来打破传统软件的“格子铺”感。界面不再是功能的堆砌，而是一个具有呼吸感的空间。

---

## 2. 色彩哲学：影调与呼吸

本系统严禁使用纯黑（#000000）或高饱和度的装饰色。我们依靠中性色的微妙推移来建立秩序。

### 调色板逻辑
- **Primary (#005eb1):** 仅用于核心操作点缀，如焦点状态或关键动作。
- **Surface 体系:** 界面深度的核心。通过 `surface-container-lowest` 到 `highest` 的切换实现逻辑分层。
- **Neutrality:** 整体基调维持在灰度区间，确保用户能够长时间专注而不产生视觉疲劳。

### “无边框”法则 (The No-Line Rule)
**禁止使用 1px 实线边框进行区域分割。** 
边界必须仅通过背景色的阶梯式位移来定义。例如：
- 在 `surface` (基础背景) 上放置 `surface-container-low` 的侧边栏。
- 在 `surface-container-low` 上放置 `surface-container-lowest` 的工作卡片。

### 材质与毛玻璃 (Glass & Mica)
为了赋予系统“灵魂”，在全局背景或悬浮层使用**云母 (Mica)** 效果。
- **Token 应用:** `surface` 或 `surface-variant` 配合 80%-90% 的不透明度，并开启 `backdrop-blur` (30px+)。
- 这能让桌面壁纸的色调隐约透出，使应用感觉像是系统原生的一部分，而非孤立的矩形框。

---

## 3. 字体系统：精密的层次

我们采用 **Segoe UI** (西文) 与 **微软雅黑** (中文) 的组合。为了体现高端感，我们拉大了 Display 与 Body 之间的字号反差。

| 等级 | Token | 尺寸 | 用途 |
| :--- | :--- | :--- | :--- |
| **Display-LG** | `display-lg` | 3.5rem | 极少使用的统计数字或欢迎语 |
| **Headline-SM** | `headline-sm` | 1.5rem | 主要功能模块的标题 |
| **Title-MD** | `title-md` | 1.125rem | 逻辑组、设置项的分类标题 |
| **Body-MD** | `body-md` | 0.875rem | 默认文本、主要输入内容 |
| **Label-SM** | `label-sm` | 0.6875rem | 辅助说明、脚注、微小元数据 |

**排版准则：**
- 标题应使用更重的字重 (Semi-bold)，并增加字母间距。
- 正文保持足够的行高 (1.5 - 1.6)，确保长文本阅读的舒适度。

---

## 4. 高级深度与光影

我们通过“影调叠层”而非物理结构来实现层级。

### 叠层原理 (The Layering Principle)
- **底层 (Base):** `surface`
- **中层 (Section):** `surface-container-low` (无阴影)
- **顶层 (Floating/Modal):** `surface-container-lowest` + **环境阴影**。

### 环境阴影 (Ambient Shadows)
当组件必须“浮起”时，严禁使用深灰色硬阴影。
- **规范:** 阴影颜色应使用 `on-surface` 颜色的 4%-8% 透明度。
- **模糊:** 扩散值 (Blur) 应设定在 20px-40px 之间，创造出一种类似自然光照射在柔光纸上的效果。

### 幽灵边框 (Ghost Border)
如果为了无障碍或极高对比度需求必须使用边框，请使用 `outline-variant` 并将不透明度调至 **10%-20%**。视觉上它应当几乎不可见，仅在近距离观察时起到界定作用。

---

## 5. 组件设计规范

### 按钮 (Buttons)
- **Primary:** 背景 `primary`，文字 `on-primary`。圆角 `DEFAULT` (4px)。
- **Secondary:** 背景 `secondary-container`，无边框。
- **Interaction:** 悬浮时背景加深 5%，点击时缩放至 0.98x，产生即时物理反馈。

### 输入字段 (Input Fields)
- **形态:** 底部 2px 的 `outline-variant` 线条，或全填充的 `surface-container-high` 容器。
- **状态:** 聚焦时，底部线条变为 `primary` 色，且容器背景轻微提亮。

### 列表与卡片 (Cards & Lists)
- **严禁使用分割线。** 
- 使用 `spacing.4` (0.9rem) 的垂直留白来区分内容项。
- 选中的列表项使用 `surface-container-highest` 背景，并配合左侧 3px 宽的 `primary` 垂直指示条。

### 交互响应 (Immediate Response)
- 去除所有超过 200ms 的过场动画。
- 使用 **Cubic-bezier(0, 0, 0, 1)** 的减速曲线，让界面切换显得干脆、专业。

---

## 6. Do's and Don'ts

### ✅ 推荐做法 (Do)
- **保持本地感:** 移除任何涉及“登录”、“同步”、“个人资料”的图标。
- **负空间平衡:** 增加左右页边距，让内容在屏幕中心“呼吸”。
- **单色图标:** 仅使用线框风格的单色图标 (如 Segoe Fluent Icons)，粗细应与文字字重匹配。

### ❌ 严禁行为 (Don't)
- **严禁大圆角:** 超过 12px 的圆角会显得过于“玩具化”，违背工具的专业感。
- **严禁高饱和度:** 除非是极其严重的错误警告 (`error`)，否则不要使用红色、黄色等高亮色。
- **严禁分割线堆砌:** 如果一个页面出现了超过 3 条横向分割线，请重新审视你的空间布局。

---

## 7. 结语

这个系统不是为了“美化”而存在，而是为了“退后”。当用户开始工作时，界面应当消失，只剩下高效的生产力流。请记住：**最好的设计是察觉不到设计的存在。**

---

## 8. 面向 ScreenTools 的落地规范

这一节不是抽象审美，而是针对当前项目的直接落地标准。目标很明确：
**让 ScreenTools 看起来像一个高端、克制、原生的桌面工具，而不是一组松散的功能面板。**

### 8.1 产品气质关键词
- **克制 (Restrained):** 不堆砌颜色，不堆砌特效，不堆砌状态。
- **精确 (Precise):** 对齐、间距、图标尺寸、文字层级必须统一。
- **轻量 (Lightweight):** 界面应像浮在桌面上的原生工具，而不是厚重的应用容器。
- **高频友好 (Operational):** 高频动作优先突出，低频设置自动退后。

### 8.2 三层视觉模型
ScreenTools 所有界面必须强制遵守三层结构，不允许自由发挥出第四层、第五层。

1. **宿主层**
   - 透明或近透明。
   - 不承载信息。
   - 不允许出现大面积纯色背景板。

2. **功能浮层**
   - 承载真正内容。
   - 使用半透明白或非常浅的中性色。
   - 阴影要轻，边界要软。

3. **强调层**
   - 仅用于当前选中态、主按钮、激活开关、核心动作提示。
   - 强调色默认统一使用 `#005EB1`。

### 8.3 颜色 Token

#### Core
- `Primary`: `#005EB1`
- `Primary-active`: `#00529B`
- `Text-primary`: `#2D3435`
- `Text-secondary`: `#4F5658`
- `Icon-secondary`: `#5F6B7A`

#### Surface
- `Surface-page`: `#F9F9F9`
- `Surface-card`: `#F2F4F4`
- `Surface-chip`: `#EEF1F2`
- `Surface-glass`: `#F0FFFFFF`
- `Surface-panel-glass`: `#F2FFFFFF`

#### Utility
- `Divider-soft`: 10%-15% opacity neutral
- `Shadow-ambient`: `#220F172A`
- `Track-off`: `#B3B9BA`

### 8.4 颜色使用规则
- 页面中**唯一允许高识别度出现的颜色是蓝色**。
- 红色只用于“正在录制”这类强状态提示。
- 不允许使用无意义的装饰色。
- 同一屏内不应同时出现超过 1 个主强调色和 1 个危险色。

### 8.5 字体层级

#### Settings 页面
- 页面标题：`24px / SemiBold`
- 分组标题：`16px / Medium`
- 设置项标题：`16px / Regular`
- 设置项说明：`15px-16px / Regular`
- 快捷键标签：`12px / Mono`

#### HUD / 悬浮条
- 品牌字：`14px / Bold`
- 操作标签：`14px / Medium`
- 工具栏标签：`10px / Medium / uppercase`
- 计时器：`56px-58px / SemiBold`

### 8.6 字体使用原则
- 中文不要所有文字都用相同字重。
- 标题更稳，说明更轻。
- 数字信息要更强，特别是计时器、时长、分辨率。
- 小标签和辅助字要主动退后，不要抢主操作视觉。

### 8.7 圆角系统
- 小按钮 / chip：`4px`
- 中等卡片：`8px`
- 浮层 / 工具栏：`12px-16px`
- 大型主卡片：`16px`
- 不允许出现超过 `16px` 的圆角，除非是开关、徽标、圆形按钮。

### 8.8 阴影系统
- 所有浮层共用一套环境阴影。
- 阴影不应发灰发脏，不应“糊”成拟态。
- 推荐：
  - `Blur`: `20-28`
  - `Depth`: `4-8`
  - `Color`: 深色低透明度

### 8.9 图标规范
- 统一使用单色线性图标。
- 粗细保持一致。
- 默认使用次级灰，不使用花哨多色图标。
- 只有当前激活状态才允许转为 `Primary`。
- 同一工具条中，图标视觉重量必须一致，不能有的粗、有的细、有的挤。

### 8.10 布局节奏

#### 组间关系
- **组与组之间留白更大**
- **组内元素之间留白更紧**

#### 视觉中心
- 每个窗口只允许有一个视觉主角：
  - 设置页：主角是内容组本身
  - 录制中 HUD：主角是计时器
  - 录制配置悬浮窗：主角是主操作按钮

### 8.11 Settings 页规范
- 重点体现“安静、可靠、长时间使用不疲劳”。
- 不要做成仪表盘，也不要做成营销型 UI。
- 快捷键只给高频操作：
  - 截图
  - 录制
  - 回录
- 低频动作继续留在设置页：
  - 保存路径
  - 开机自启动
  - 音频偏好
  - 画质

### 8.12 HUD 规范
- HUD 必须像“浮在桌面上的工具”，而不是独立应用窗口。
- 背景尽量透明，信息层尽量轻。
- 主控制条里：
  - 当前模式必须最清楚
  - 次级工具必须退后
  - 不可堆砌图标

### 8.13 Recording Active 浮层规范
- 视觉中心永远是时间。
- 音量条只表达反馈，不抢主视觉。
- 暂停和停止必须一眼可达，但不能显得笨重。
- 更多按钮必须存在，但必须弱化。

### 8.14 禁止事项
- 禁止大面积灰块叠灰块。
- 禁止多个阴影逻辑并存。
- 禁止一个页面同时出现太多蓝色块。
- 禁止为了“精致”加入无意义描边。
- 禁止把所有功能都快捷键化。
- 禁止图标风格混乱。

### 8.15 实施优先级
后续所有 UI 调整，按下面顺序做，不要反过来：

1. 先调布局与留白
2. 再调文字层级
3. 再调颜色与材质
4. 最后才调图标和阴影

如果顺序反了，界面很容易变成“局部精致，整体混乱”。
